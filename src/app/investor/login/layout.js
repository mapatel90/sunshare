import LanguageSelector from '@/components/shared/LanguageSelector'

export default function InvestorLoginLayout({ children }) {
  return (
    <div className="auth-layout">
      {/* <LanguageSelector /> */}
      {children}
    </div>
  )
}
